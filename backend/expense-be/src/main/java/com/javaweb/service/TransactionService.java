package com.javaweb.service;

//import model.TransactionDTO;
import java.util.List;

public class TransactionService {
//    private final TransactionRepositoryImpl transactionRepositoryImpl;
//
//    public TransactionService(TransactionRepositoryImpl transactionRepositoryImpl) {
//        this.transactionRepositoryImpl = transactionRepositoryImpl;
//    }
//
//    public List<TransactionDTO> getTransactionsByUserId(Long userId) {
//        return transactionRepositoryImpl.getTransactionsByUserId(userId).stream()
//                .map(entity -> new TransactionDTO(
//                        entity.getId(),
//                        entity.getUserTransaction().getId(),
//                        entity.getWalletTransaction().getId(),
//                        entity.getGoalTransaction() != null ? entity.getGoalTransaction().getId() : null,
//                        entity.getBorrowingTransaction() != null ? entity.getBorrowingTransaction().getId() : null,
//                        entity.getCategoryTransaction().getId(),
//                        entity.getAmount(),
//                        entity.getType(),
//                        entity.getDescription(),
//                        entity.getIsRecurring(),
//                        entity.getStatus(),
//                        entity.getCreatedAt()
//                )).toList();
//    }
}
